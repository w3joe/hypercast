# Latest user steering: fair hyperparameter tuning

2026-09-11: the user asked for findings and correctly raised hyperparameter tuning. This supplements the remaining-model breadth request; the original three backbones remain excluded and the cumulative additional allocation remains $20 with a $1 reserve.

Current evidence: the original three-model development already gave every arm the same two learning rates (.0003 and .001), with one paired seed and up to 150 epochs selected by inner stopping. Dimension rankings changed with learning rate. The expanded study currently has only native five-epoch calibrations plus all-variant numerical checks. These cannot establish a trained hypercomplex accuracy ranking.

After calibration and repair gates, prioritize a balanced, longer, two-rate tuning stage over automatically spending on 84 more five-epoch fits. The 96-configuration breadth target remains twelve backbones times native, controlled real, 2D, 4D, 8D and three low-rank controls. The frozen five-epoch manifest is a historical proposal, not authorization to ignore this newer tuning priority. Document its supersession before submitting a new stage.

Proposed search: learning rates .0003 and .001, the same number of trials and maximum epoch allowance for every arm within each backbone, paired seed 701, context 32/horizon 5, chronological inner checkpoint selection, train-only scaling and restored best weights. Keep architecture width, dropout, optimizer family and parameter counts fixed so tuning cannot silently change the comparison. Learning rate and enough training come first; a later equally budgeted regularization search is conditional on remaining funds. Do not tune only the hypercomplex arms or give a promising dimension extra attempts.

The complete two-rate matrix has 192 fits. Do not launch it before costing all fits from the twelve observed calibration timings, conservatively allowing slower hypercomplex variants, all attempts, CPU/memory, startup, 3x uncertainty and the reserve. Consider batching several sequential fits into one bounded L4 job to reduce repeated launch overhead, only after validating per-fit audits, partial-failure accounting and complete artifact preservation. Never increase simultaneous GPUs. If the 150-epoch ceiling does not fit, predeclare a uniform lower ceiling (for example 50 or 25) before scoring and disclose that convergence may remain incomplete. If no balanced useful stage fits, report the funding limitation; do not silently drop models or controls.

Pilot scores do not select or eliminate arms. Final efficacy comparisons need later paired seeds/temporal robustness with choices frozen from development, adequate convergence, and dependence-aware multiplicity correction. A best tuning result on the already-seen Copper period remains exploratory and cannot be called independent confirmation.

Operational finding at this review: seven calibrations passed, while SCINet stopped before training at its GPU identical-real output gate (maximum absolute difference about 0.0003024). No relaxed tolerance is authorized. The investigated cause is unequal default precision between convolution and matrix multiplication; PyTorch documents TF32 convolution behavior in https://docs.pytorch.org/docs/main/notes/numerical_accuracy.html. Version source and preserve the failed attempt's cost before retrying with equal IEEE FP32 precision for every SCINet arm, including its training path. Resume only if the original numerical tolerance passes. Other completed calibrations are not repeated.
