# Conditional follow-up after the complete 192-fit tuning stage

Draft recorded at the 2026-09-11 09:55 UTC supervision review; **not submitted or frozen as an executable stage**. FiLM is still running, so its results and timings must be included before selecting any rates or committing this proposal. No active worker source changes are allowed.

Current audit: 176 fits from eleven models, all passing, with 75 selecting their final epoch. Hypercomplex performance is model dependent. Apparent gains must be tested with more training and fresh initialization seeds; several low-rank controls remain as good or better, and every currently selected hypercomplex result remains worse than persistence.

Preferred next question: do the model-specific accuracy/parameter trade-offs survive additional training and fresh seeds when learning rates are fixed in advance? Retain all twelve remaining backbones and all eight arms. Do not repeat the excluded original three or select only promising dimensions/models.

Candidate design: freeze each arm's learning rate from the complete two-rate development grid using the previously declared MAE/persistence rule and .1% lower-rate tie break. Use fresh paired seeds 401 and 503 and a uniform 50-epoch maximum with the same inner stopping, input data, model sites, precision controls and baselines. This would be 192 fits: twelve backbones x eight arms x two seeds. It tests seed stability at a longer common training allowance, not independent generalization or a clean isolated effect of epoch count. Copper validation dates have already been used for rate selection, so do not relabel them as held-out data.

After FiLM completes, cost the complete candidate stage from measured per-arm seconds per executed epoch, allow overhead and slower seeds, and reserve every batch's bounded timeout plus controller overhead at the existing 3x uncertainty rate. Include the existing ledger, $1 reserve, any failures and an additional planning margin. Fifty epochs/two seeds is conditional on affordability. If it cannot fit, compare uniformly smaller alternatives before any scoring; do not silently remove models or controls, increase the budget, or launch an incomplete matrix.

Implementation would need a separate explicit, hashed replication configuration containing the eight fixed rates and the two seeds. Preserve historical tuning semantics and candidate hashes. Each child must contain exactly one variant/rate/seed and return separate artifacts. Audit untouched parameter pairing within each seed, distinct states across seeds, identical data/scalers, checkpoint restoration, partial-failure manifests and sixteen-fit counting. Do not overload the original fixed two-learning-rate flag or duplicate successful child fits after a partial failure. Wait for jobs and controller to stop, reconcile costs, and version provenance before any source edit.

Before this stage's scores are inspected, preregister descriptive mean and seed range, paired origin-level errors averaged across the two seeds, and dependence-aware uncertainty across dates. The 180 primary contrasts and family-wise multiplicity must remain visible. Set temporal block length from development only (at least the five-step horizon), record half/double sensitivity, and do not count overlapping leads or repeated seeds as independent observations. A nonsignificant difference is not equivalence. Sparse seeds and reused dates limit conclusions even if a confidence interval excludes zero.

At minimum, finish the complete current tuning review and report convergence, all controls, absolute error relative to persistence, whole-model parameters, runtime and failures. A follow-up is optional if it cannot answer a useful question within the remaining allocation; spending the entire budget is not itself a scientific goal.
